# Demo flow — Step 2: Full approval ingest

**User action:** Upload research package via ingestion console (controlled UI trigger).
**Ingest:** 5 OA articles — see `ingest/` (xml + json per PMC ID).
**Expected outcome:** License check passes; metadata linking succeeds; curator approves; 9 entities persisted to KB.
**Legacy ID:** ING-001
